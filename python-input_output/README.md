# Python - Input/Output

This project contains solutions for Python Input/Output tasks.

## Task 0 - Read file

The function `read_file(filename="")` opens a UTF-8 text file using the `with` statement, reads its content, and prints it to the standard output.

### Prototype

```python
def read_file(filename="")
```

### Requirements

- Use the `with` statement.
- Do not import any module.
- Do not handle file exceptions.
